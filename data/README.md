# Data Folder

The raw, interim, and processed datasets are not included in this GitHub repo because they are large and can be recreated through the project pipeline.

The project documentation explains the data sources, pipeline order, validation checks, and output files.

Main local data folders used during development:

- data/raw
- data/interim
- data/processed
